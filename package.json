{
  "name": "github-stats-api",
  "version": "1.0.0",
  "description": "Production-ready GitHub profile cards API for Vercel and Bun",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "typecheck": "tsc --noEmit",
    "start": "bun run src/server.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0",
    "@types/bun": "latest"
  },
  "engines": {
    "node": "22.x"
  },
  "module": "dist/server.js",
  "private": true
}
